// Directions: Create a variable fullName and assign it your full name as a string. Print the value of the variable in the console.

// Running code run node name.js in the terminal.

const fullName = "Darnell";

console.log(fullName)